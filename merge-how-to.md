Получаем ветку с сервера ( и вообще все изменения ) так:
`git fetch`.

Для слияния веток делаем следующее:
`git merge second_branch_name` ( сами находимся в `first_branch_name` ).

Как обычно, отправляем изменения на сервер:
`git push`
